    <div  class="paginate d-flex justify-content-center" style="margin-top:50px;">
        <%= paginate @posts, theme:'bootstrap-5',
            pagination_class: "pagination-sm flex-wrap justify-content-center pagination-centered",
            nav_class: "d-inline-block", remote:true %>
    </div>
    <div id="box-posts" class="box-posts">
        <%= render @posts %>
    </div>
    <div class="paginate d-flex justify-content-center" style="margin-bottom:100px;">
        <%= paginate @posts, theme:'bootstrap-5',
            pagination_class: "pagination-sm flex-wrap justify-content-center pagination-centered",
            nav_class: "d-inline-block", remote:true %>
    </div>
<%= render './comments/modal_comments' %>