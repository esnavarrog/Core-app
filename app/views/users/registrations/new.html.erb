<div class="container container-girls-session">
  <div class="d-flex justify-content-center align-content-center">
    <h2 class="mb-0"><%= inline_svg_tag('logomitarde.svg', class:'logo-header') %></h2>
  </div>
  <div class="link-bottom-users mt-3">
    <%= render "users/shared/links_top" %>
    
  </div>
  <div class="card my-3">
    <div class="card-body">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "users/shared/error_messages", resource: resource %>

        <div class="form-group mb-3">
          <%= f.label :email %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class:'form-control' %>
        </div>

        <div class="form-group mb-3">
          <%= f.label :nickname %><br />
          <%= f.text_field :nickname, autocomplete: false, class:'form-control' %>
        </div>
        <div class="form-group mb-3">
          <%= f.label :avatar, class:'d-flex align-items-center justify-content-center', id:'button-upload-avatar' do %>
          <i class="bi bi-upload me-2"></i> Sube tu avatar
          <% end %>
              
          <%= f.file_field :avatar, class:'d-none' %>

        </div>

        <div class="form-group mb-3">
          <%= f.label :password %>
          <% if @minimum_password_length %>
          <em>(mínimo <%= @minimum_password_length %> caracteres)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "new-password", class:'form-control' %>
        </div>

        <div class="form-group mb-3">
          <%= f.label :password_confirmation %><br />
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class:'form-control' %>
        </div>

        <%= flash[:recaptcha_error] %>
        <%= recaptcha_tags %>
        
        <div class="d-grid mb-3 mt-3">
          <%= f.submit "Crear Cuenta", class:'btn btn-primary btn-lg' %>
        </div>
      <% end %>

    </div>
  </div>
  <div class="link-bottom-users">
    <%= render "users/shared/links" %>
    <%= link_to :root do %>
      <%= inline_svg_tag('icons/icon_arrow_left.svg') %>
      Volver al Home
    <% end %>
  </div>
</div>
